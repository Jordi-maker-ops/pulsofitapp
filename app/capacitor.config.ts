import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.pulsofit.fitness',
  appName: 'PulsoFit',
  webDir: 'dist',
  backgroundColor: '#0C0D12',
}

export default config
